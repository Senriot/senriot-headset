apply plugin: "war"

bootWar {
    mainClassName = "com.senriot.quickup.QuickupApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
